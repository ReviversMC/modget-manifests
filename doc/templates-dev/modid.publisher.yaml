# schema goes here

manifestSpecVersion: "5.0-beta.14"

names: #(Names!)
  official: #(String!)
  others: #(List<String!>?)
    - #(String!)
publisher: #(String!)
authors: #(List<String!>?)
  - #(String!)
description:
iconUrls:
  -  #Prefer Modrinth, then CF, then source control (String?)

releasePlatforms: # It should be removed from the versions if moved back here.
  curseforgeID: #Project ID (String?)
  modrinthID: #Project ID (String?)
  sourceControlUrl: #Full URL (String?) Is this even necessary, as we have source listed as a field?
   # (cloud: Let's standardize on having `.git`?) (fire: sure)

status:
updatedAlternatives:
  - packageId:

homeUrl: #(String?)
sourceUrl: #(String?)
issuesUrl: #(String?)
supportUrl: #(String?)
wikiUrl: #(String?)
chatUrls: #(ChatUrls!)
  discordUrl: #(String?)
  ircUrl: #(String?)
  otherUrls: #(Other?)
    - name: #(String!)
      url: #(String!)
donationUrls:
  patreonUrl:
  kofiUrl:
  otherUrls:
    - name:
      url:

supportedLoaders: #(cloud: replace open array with fixed loaders, as MG is only supposed to support Fabric and Forge. fire: and liteloader and rift)
  fabric: #(Boolean!)
  forge: #(Boolean!)
  rift: #(Boolean!)
  meddle: #(Boolean!)
  m3l: #(Boolean!)
  rope: #(Boolean!)
  blazeloader: #(Boolean!)
  liteloader: #(Boolean!)
  risugami: #(Boolean!)

versions:
  - version: #Version extracted from fabric.mod.json (String!)
    semverVersion:
